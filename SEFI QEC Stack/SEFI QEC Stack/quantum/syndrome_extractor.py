class SyndromeExtractor:

    def extract(
        self,
        transmitted,
        received
    ):

        syndrome = []

        for i in range(len(transmitted)):

            if transmitted[i] != receivedsyndrome.append(i)

        return syndrome